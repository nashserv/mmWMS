# Зависимости ТЕСТОВ, а не сервисов.
#
# Ставятся поверх образа сервиса в тестовом контуре и в CI; в сам образ они не
# попадают. Раздел 2.2 мастера замораживает набор зависимостей приложения —
# это про приложение, а не про то, чем его проверяют.
#
# Версии закреплены до патча: «тесты зелёные» обязано значить одно и то же в
# понедельник и в пятницу.
pytest==8.3.4
pytest-asyncio==0.25.0
# Проверка ответов и запросов по openapi.yaml / asyncapi.yaml.
jsonschema==4.23.0
referencing==0.35.1
PyYAML==6.0.2
