# Поток A — находки, требующие решения потока 0 или владельца

Здесь только то, что поток A не имеет права решить сам: расхождения внутри
замороженных документов и дыры в контракте. Правило 9.5.2 — контракт после
заморозки меняет поток 0.

Каждая находка описана так, чтобы её можно было закрыть, не перечитывая код:
что написано, что из этого следует, что сделано сейчас и чего это стоит.

---

## 1. `available = good − reserved − buffer` вычитает резерв дважды

**Что написано.** Раздел 6.2 мастера кладёт в транзакцию резерва движение
`good → reserved`. Значит, в проекции `stock_balance` состояние `good` после
резерва этот резерв уже не содержит — товар физически переехал в другое
состояние.

Раздел 6.4, инвариант 7 и шаг 15 полного прогона задают формулу публикации
`available = good − reserved − buffer(sku)`, читая оба состояния из той же
проекции.

**Что из этого следует.** Резерв вычитается второй раз. При 10 единицах и
резерве на 3 в Wildberries уезжает `(10−3) − 3 − 0 = 4`, а физически свободно
7. Публикуется меньше, чем есть.

**Что сделано сейчас.** Формула реализована буквально, как написано
(`app/service.py`, `StockOperations.available_rows`). Обоснование: обе стороны
расхождения сходятся в том, что остаток в WB занижают всегда, а двойное
вычитание ошибается в безопасную сторону — недопродажа, а не продажа
несуществующего. Плюс шаг 15 прогона проверяет именно эту формулу, а тесты
пишутся против контракта (правило 9.5.3).

**Чего это стоит.** Пока по SKU есть незакрытые резервы, WB видит меньше
товара. Самоустраняется по мере отгрузки: `shipped` уводит товар из
`reserved`. При 350 заданиях в день на 2322 товара эффект небольшой, но он не
нулевой и это потерянные продажи клиента.

**Что решить.** Либо `available = good − buffer` (резерв уже вычтен движением),
либо оставить как есть сознательно. Это вопрос к владельцу, а не к коду:
цена ошибки в одну сторону — недопродажа, в другую — продажа того, чего нет.

> **ЗАКРЫТО (интеграция, мастер 1.3).** Принята формула
> `available = good − buffer`. Двойное вычитание признано ошибкой переписывания,
> а не предохранителем: занижает `buffer`, страховой запас владельца, и этого
> достаточно для «всегда занижать». Исправлены раздел 6.4, инвариант 7, ASSERT
> шага 15, `available_for_push`/`available_rows`, заглушка и шаг 15 прогона.
> То же расхождение независимо нашёл поток 0 (`docs/schema-decisions.md`, 3.1).

---

## 2. Приход товара нечем описать событием

**Что написано.** Инвариант 13: «физическое действие = движение + событие +
начисление». Единственное событие о движении товара в приложении E —
`inventory.movement.recorded.v1`, и у него обязательны `order_id` (integer) и
`status` (`TaskStatus`).

**Что из этого следует.** Начальный остаток и приёмка — физические действия
без заказа. Положить в обязательный `order_id` нечего. Список
`WMS_EVENT_TYPES` закрыт и валидируется при эмиссии, поэтому завести своё
событие поток A не может и не должен.

**Что сделано сейчас.** `/warehouse/documents` пишет движения в журнал и
события не эмитит. На полном прогоне это ничего не ломает: шаг 12 тарифицирует
только `wb.supply.shipped.v1`, `order.packed.v1` и `wb.orders.processed.v1`.

**Чего это стоит.** Раздел 3.4 перечисляет приёмку и хранение среди
нетарифицируемых сегодня услуг — «не тарифицируются: хранение, приёмка,
стикеровка, упаковочные материалы, возвраты». Когда поток C дойдёт до них,
события не окажется.

**Что решить.** Нужно ли событие приёмки (например,
`inventory.receipt.recorded.v1`) в каталоге приложения A и в `WMS_EVENT_TYPES`.

> **ЗАКРЫТО (интеграция, мастер 1.3).** Заведено `wms.receipt.completed.v1` —
> приложение A, схема в приложении E, `WMS_EVENT_TYPES`, канал в asyncapi.
> `/receipts` эмитит его той же транзакцией, что и движения, и только для
> завершённой приёмки. То же самое независимо просил поток C (пункт 2): ему
> приёмку было нечем тарифицировать.

---

## 3. Маршрута создания зон и ячеек в контракте нет

**Что написано.** Приложение B перечисляет 29 маршрутов плюс новые; ни одного
про `zone`/`cell`. При этом шаг 1 прогона требует «завести клиента, ячейки», а
`WarehouseDocumentLine` и `ReceiptLine` адресуют строки полем `cell_address`.
README прогона называет это открытой дырой контракта.

**Что сделано сейчас.** Ячейка заводится при первом упоминании адреса
(`repositories.ensure_cell`): зона `STORAGE` вида `storage` на складе `RUM`,
`route_order` пустой. Это ровно второй из двух вариантов, которые README
прогона называет допустимыми — «явное правило: ячейка создаётся по адресу при
первом упоминании».

**Чего это стоит.** `route_order` остаётся пустым, и лист подбора сортировать
не по чему — сборщик пойдёт по складу зигзагом. Это упирается в вопрос 1
раздела 13 мастера, который открыт: схема зон и формат адреса не утверждены.

**Что решить.** Вопрос 1 раздела 13 (схема зон, формат адреса, порядок
обхода). Пока он открыт, ячейки заводятся вслепую — это названо несущим
вопросом для потока A в самом мастере.

> **ОТЛОЖЕНО СОЗНАТЕЛЬНО: это вопрос к владельцу, а не к коду.** Раздел 13,
> вопрос 1, остаётся открытым — интегратор его не закрывает и закрыть не может.
> Принятое поведение («ячейка заводится по адресу при первом упоминании»)
> оставлено как есть: это второй из двух вариантов, которые сам README прогона
> называет допустимыми. Цена известна и записана: `route_order` пуст, лист
> подбора сортировать не по чему.

---

## 4. Тесты ядра требуют Postgres, а job `tests` в CI его не поднимает

**Что написано.** `.github/workflows/ci.yml`, job `tests`: ставит зависимости и
гоняет `pytest` в `services/wms`. Postgres поднимает только job `migrations`.

**Что из этого следует.** Транзакцию раздела 6.2 нельзя проверить без базы: её
смысл — блокировки, триггеры проекции и `ON CONFLICT`. Тесты
`test_reservation_transaction.py` и `test_real_api.py` без `DATABASE_URL`
пропускаются, а пропуск читается как успех.

**Что сделано сейчас.** Тесты берут адрес из `WMS_TEST_DATABASE_URL` или
`DATABASE_URL` и без него пропускаются с внятной причиной. Локально гоняются
против настоящей базы стенда перед каждым коммитом.

**Что решить.** Добавить в job `tests` тот же `services: postgres`, что уже
есть в job `migrations`, накатить миграции и передать `DATABASE_URL`. Файл
общий, поток A его не трогает (правило 9.5.1) — заявка потоку 0.

> **ЗАКРЫТО (интеграция).** Сделано. Заодно вскрылось, что тринадцать тестов
> ядра разговаривают с Wildberries через симулятор и без него тоже
> пропускались бы: симулятор теперь поднимается шагом job'а, а
> `WB_SIMULATOR_URL` передаётся явно — у проверки «симулятор поднят» и у самого
> клиента WB были разные умолчания.

---

## 5. Шаг 4 прогона возвращается на первом же появившемся задании

**Что написано.** Шаг 4: «ASSERT 5 wms_task за < 2 с». Ожидание построено так:

```python
tasks = wait_until(
    lambda: db.rows("SELECT id, state FROM wms_task WHERE wb_order_id = ANY(%s)",
                    (order_ids,)) or None,
    timeout_s=TASK_VISIBLE_S, interval_s=0.05)
...
assert tasks and len(tasks) == data.WB_ORDERS
```

`wait_until` возвращает первое истинное значение, а список из одной строки уже
истинен. То есть ожидание заканчивается, как только появилось ПЕРВОЕ задание,
а проверяется, что появились все пять.

**Что из этого следует.** Шаг зеленеет, только если все пять заданий окажутся в
базе между двумя опросами наблюдателя, то есть внутри 50 мс. Замер на стенде:

```
900211 | 26:38.199 | +1404.0 мс   ← начало пачки (интервал опроса кабинета)
900212 | 26:38.213 |   +14.4 мс
900213 | 26:38.218 |    +4.6 мс
900214 | 26:38.222 |    +4.2 мс
900215 | 26:38.226 |    +3.7 мс   ← вся пятёрка за 27 мс
```

Пять заданий заводятся за 27 мс, по 4 мс на задание. Наблюдатель опрашивает раз
в 50 мс, поэтому примерно в 40 % запусков его опрос попадает внутрь пачки и
возвращает 1–4 задания из 5. Пять прогонов подряд: 1, 4, 3, 4 и один зелёный по
этому пункту.

**Почему это не чинится со стороны потока A.** Чтобы попадание внутрь пачки
стало редким, пятёрка должна появляться атомарно — то есть все пять заданий
одной транзакцией. Это прямо противоречит инварианту 4: транзакция удерживала
бы блокировки строк остатка на все пять заказов сразу, а на странице WB их до
1000. Пятьдесят заказов в одной транзакции — это 200 мс удержания при пределе
в 100 мс, и всё это время конкурирующие сборщики стоят.

Требование «задание доступно сразу» (инвариант 8) выполняется: от появления в
симуляторе до строки в базе проходит меньше 2 с, и это проверяется тестом
`services/wms/tests/test_wb_sync.py::test_orders_become_tasks_and_reservations_without_a_queue`,
который ждёт именно все пять.

> **ЗАКРЫТО (интеграция).** Предикат ждёт полный набор.

**Что решить.** Ждать полный набор, а не первое появление:

```python
def all_arrived():
    found = db.rows("SELECT id, state FROM wms_task WHERE wb_order_id = ANY(%s)", (order_ids,))
    return found if len(found) == data.WB_ORDERS else None

tasks = wait_until(all_arrived, timeout_s=TASK_VISIBLE_S, interval_s=0.05)
```

Смысл шага при этом не меняется — меняется только то, что он перестаёт мерить
удачу попадания между опросами.

---

## 6. Проверка «ни одного idle in transaction» ловит любую многошаговую транзакцию

**Что написано.** Шаг 4 требует: `assert not watch.idle_in_transaction`. Замысел
описан в README прогона верно — «бэкенд сидит `idle in transaction` и ждёт
клиента, пока тот разговаривает с Wildberries».

**Что из этого следует.** В состоянии `idle in transaction` бэкенд оказывается
не только из-за сетевого вызова, но и в обычном промежутке между двумя
операторами одной транзакции: сервер выполнил `INSERT`, ответил и ждёт
следующего сообщения клиента. Транзакция раздела 6.2 многошаговая по самому
своему описанию в мастере — пять операторов между BEGIN и COMMIT. Наблюдатель с
шагом 5 мс неизбежно попадает в такие промежутки.

**Что сделано сейчас.** Всё, что можно было убрать, убрано:

- публикация в RabbitMQ вынесена из транзакции публикатора outbox (это была
  настоящая находка прогона — пачка стояла под `FOR UPDATE SKIP LOCKED`, пока
  pika ходила в шину);
- маршрут `/reservations` и опросчик больше не шлют события сами: они уже
  лежат в outbox, а слали их вдобавок — каждое событие уезжало в шину дважды,
  и десять миллисекунд разговора с брокером ложились на горячий путь резерва;
- одиночные запросы воркеров выполняются вообще без явной транзакции
  (`postgres.single`), а четыре обращения опросчика к базе за цикл сведены к
  двум одним CTE каждое.

Замер на стенде до и после, 20 секунд наблюдения с шагом 5 мс:

```
было:  49 попаданий на ~4000 замеров, все — фоновые транзакции опросчика
стало:  8 попаданий на 3763 замера, из них 6 — состояние сразу после BEGIN
```

**Чего это стоит.** Остаток — промежутки внутри самой транзакции 6.2. Убрать их
можно, только сделав резерв одним оператором на стороне сервера, то есть
переписав ядро в нечитаемый CTE или в хранимую функцию ради метрики.

> **ЗАКРЫТО (интеграция).** Порог 50 мс, как вы и предлагали.

**Что решить.** Отличать долгое ожидание клиента от промежутка между
операторами — например, считать нарушением только `idle in transaction` с
возрастом транзакции выше порога:

```python
return [s for s in self.samples
        if s.state == "idle in transaction" and s.age_ms > 50]
```

Вызов в Wildberries занимает около 500 мс и через такой порог проходит с
запасом; промежуток между двумя операторами — нет. Соседняя проверка того же
шага (`max_age_ms < 100 мс`) уже устроена именно так и работает.

---

## 7. `assignee` в схеме uuid, в контракте строка до 128 символов

**Что написано.** Миграция 003: `assignee uuid`. Контракт,
`TasksPullParams.assignee`: `type: string, minLength 1, maxLength 128`,
«Сборщик или приёмщик, за которым закрепляются задания». Полный прогон
присылает `picker-1`, `picker-no-bus`.

**Что из этого следует.** `picker-1` в колонку `uuid` не ложится. Замысел
схемы понятен и правилен — `identity` единственный источник пользователей
(раздел 12), и там идентификатор пользователя это uuid. Но контракт разрешает
любую строку, и прогон этим пользуется.

**Что сделано сейчас.** Принимаются оба вида: строка, разбирающаяся как uuid,
идёт как есть; всё остальное разворачивается в постоянный `uuid5` от имени
(`tasks.assignee_id`). Одно и то же имя даёт один и тот же uuid от смены к
смене, так что «за кем задание» остаётся воспроизводимым.

**Чего это стоит.** У задания, выданного по имени рабочего места, в базе
лежит синтетический uuid, который не резолвится в `identity`. В бою этого не
случится — там придёт настоящий идентификатор пользователя, — но на стенде в
`wms_task.assignee` стоят значения, по которым человека не найти.

**Что решить.** Либо сузить контракт до uuid (тогда прогон обязан присылать
идентификаторы, а не имена), либо признать имя рабочего места законным
значением и завести для него место в схеме.

> **ЗАКРЫТО (интеграция, мастер 1.3): сужен до uuid.** Плюс нашлась вторая
> половина той же беды: рабочее место слало в это поле `workstation-screen`,
> потому что оно было обязательным, а экран при `claim: false` ничего не
> занимает. Выдуманный пользователь для не-человека — это ровно та строка,
> которую потом не найти в `identity`. Поэтому `assignee` теперь обязателен
> **только при `claim: true`**. Прогон шлёт постоянные uuid сборщиков.
> `assignee_id` с его uuid5 оставлен как терпимость к старым клиентам.

---

## 8. Прогон копит задания между запусками и травит шаг 8

**Что написано.** README прогона: «Прогон заводит своего клиента
(`full-run-seller`), свои штрихкоды, ячейки и коробки и не опирается на дамп:
вчерашний остаток не должен превращаться в сегодняшний ложный зелёный».

**Что из этого следует.** Клиент один и тот же между запусками, а задания
после запуска остаются. Больше всего их оставляет шаг 16: нагрузка заводит
около 160 заданий в состоянии `reserved`, и никто их не разбирает — шаги 9–11
принадлежат потоку B и на срезе `-m stream_a` не выполняются вовсе.

На следующем запуске шаг 8 просит пять сессий подбора забрать по пять заданий.
Выдача идёт по сроку WB, и первыми уходят задания вчерашнего прогона, а не
сегодняшнего. Шаг краснеет с «задания не выданы никому», хотя выдача работает.

Замер: после нескольких прогонов у `full-run-seller` накопилось 283 задания в
`reserved`, из них 36 свободных. После отмены накопленного шаг 8 зеленеет с
первого раза.

**Чего это стоит.** Ложный красный, растущий с числом прогонов. Он же скрывает
настоящие поломки выдачи: шаг всё равно красный, и разбирать его перестанут.

> **ЗАКРЫТО (интеграция), обоими способами сразу.** Прогон отменяет
> незакрытые задания своих клиентов до и после запуска (отмена законна и с
> причиной, инвариант 11). А нагрузочный шаг получил своего клиента и свой
> кабинет — его задания больше не попадают в очередь шага 8 в принципе, и его
> вызовы не выбирают лимит кабинета, на котором меряется шаг 4.

**Что решить.** Любой из двух способов:

- фильтровать выдачу по заданиям этого запуска — шаг 8 знает их
  `ctx["task_ids"]` и может передать `owner_external_ids` вместе с явным
  списком, либо сверять пересечение, а не полное покрытие;
- либо разбирать за собой: закрывать задания нагрузочного шага отменой в конце
  прогона. Отмена — законная операция с причиной, товар при ней возвращается
  на полку, и стенд остаётся в том же состоянии, в каком прогон его застал.

**То же место цепляет и шаг 9, а через него шаг 11.** Шаг 8 тянет задания без
фильтра по товару, а в очереди рядом с пятёркой шага 4 лежит задание шага 6 —
на `ZERO_STOCK_BARCODE`. Шаг 9 берёт из выданного первое попавшееся
(`next(iter(pulled.values()))[0]`) и сканирует его `BARCODES[0]`, считая, что
это задание шага 4. Если ему досталось задание шага 6, свой штрихкод не
совпадает и шаг краснеет — а за ним и шаг 11, которому нужен
`packed_task_ids`. Наблюдалось на стенде: два прогона подряд, в одном шаг 9
зелёный, в другом красный, при одном и том же коде.

Лечится тем же — выбирать задание из `ctx["task_ids"]`, а не из произвольной
сессии.

---

## 9. Рабочее место негде завести: маршрута для `station` в контракте нет

**Что написано.** Таблица `station` есть в схеме (миграция 001): имя, принтер,
транспорт, признак активности. Шаг 10 прогона берёт станцию прямо из базы:

```python
station_id = db.value("SELECT id FROM station WHERE active ORDER BY name LIMIT 1")
assert station_id, "на стенде не заведено ни одной станции — печатать некуда"
```

Ни в приложении B, ни среди новых маршрутов ничего про станции нет. Завести её
через контракт нельзя ни потоку A, ни потоку B.

**Что из этого следует.** Шаг 10 краснеет до всякой печати — на строке поиска
станции. Обе половины пути при этом работают: `/labels/{task_id}/print`
отдаёт готовые байты, а `station_id` в запросе принимается любой.

**Что сделано сейчас.** Маршрут печати не требует, чтобы станция была
заведена: если её нет, транспорт считается `agent` — принтеры USB, решение
владельца 14. Своя половина бюджета измерена тестом
`test_printing_hands_ready_bytes_to_the_station`: десять печатей подряд,
худшая под 25 мс при пределе шага в 50 мс на весь путь до устройства.

**Что решить.** Либо маршрут `/stations` в контракте (завести, погасить,
перечислить), либо явное правило «станции заводятся при развёртывании стенда»
и строки в сиде. Второе дешевле и, судя по разделу 4, ближе к жизни: станций
пять, и меняются они не чаще, чем переезжает склад.

> **ЗАКРЫТО (интеграция): выбран второй, как вы и предлагали.** Пять станций в
> `infrastructure/stand/seed/stations.sql`, правило записано в контракт явным
> текстом при `station_id`. То же самое просил поток B (заявка 5).

---

## 10. Стенд поднимает заглушку, а не сервис

**Что написано.** `infrastructure/stand/compose.yaml`, сервис `wms`:
`WMS_MOCK: "true"` с комментарием «Поток A снимает флаг вместе с mock».
Воркеров в compose стенда нет вовсе — на момент потока 0 их не существовало.

**Что из этого следует.** Настоящий сервис на стенде сам собой не поднимется:
нужно снять флаг и добавить четыре воркера. Файл общий, поток A его не трогает
(правило 9.5.1).

**Что сделано сейчас.** В `services/wms/compose.yaml` лежит готовое описание
сервиса и всех четырёх воркеров с теми же переменными, что у стенда. Его можно
подключить из compose стенда через `include` либо перенести блоки руками.
Заглушка при этом никуда не девается: тот же образ поднимает её по
`WMS_MOCK=true`, и потоки B и C переключаются флагом, а не пересборкой
(правило 9.5.4).

**Что решить.** Кто и когда переводит стенд на настоящий сервис. Пока потоки B
и C пишут клиентов против заглушки, спешить некуда; но полный прогон уже сейчас
имеет смысл гонять против настоящего — иначе шаги 2–16 меряют заглушку.

> **ЗАКРЫТО (интеграция).** Стенд поднимает настоящий сервис и все четыре
> воркера одним `compose.yaml`; наложения потоков B и C переехали туда же и
> удалены. Заглушка никуда не делась — возвращается переменной `WMS_MOCK`, а не
> пересборкой. Полный прогон: 16 из 16.

Проверено на стенде: `WMS_MOCK=false` с четырьмя воркерами даёт 13 зелёных из
16, против 0 у заглушки на срезе потока A.

---

## 11. Адаптеров `product-catalog` и `wb-returns` в репозитории нет

**Что написано.** Файл `02-stream-a-wms-wildberries.md`: поток A владеет
`services/wms/` целиком **и адаптерами в `product-catalog` и `wb-returns`** —
той их частью, что зовёт `wms` вместо Odoo.

**Что из этого следует.** Этих сервисов в репозитории нет:
`infrastructure/stand/README.md` прямо говорит, что их исходники лежат на
боевом сервере (`/root/MM-express-final-platform-integration`), и выдумывать
их описание значит записать в стенд неправду. Править нечего.

**Что сделано вместо.** Закрыта та сторона, которая от потока A зависит:
контракт `/api/mmx/wms/v1/*` реализован целиком, все 35 маршрутов приложения B
отвечают по-настоящему. Именно ради этого контракт и сохранялся — под него уже
написаны клиенты (раздел 6.7). По README стенда переключение этих сервисов
сводится к конфигурации, а не к коду:

```yaml
WMS_ADAPTER: http
ODOO_WMS_URL: http://wms:8080/api/mmx/wms/v1
```

**Что останется правкой кода, когда исходники появятся.** Две вещи, и обе про
`product-catalog`:

1. **Снимки остатков из каталога уходят** (раздел 6.8): `wms` публикует
   остатки сама, немедленно после движения. `catalog-stock-worker` исчезает
   (раздел 6.3), и пока он жив, два сервиса будут писать в один кабинет
   разные числа.
2. **Карточки Wildberries каталог берёт через `wms`** (раздел 6.8) — а
   маршрута для карточек в контракте нет. Приложение B знает
   `/catalog/products`, `/catalog/products/ensure`, `/catalog/stocks`,
   `/catalog/stocks/bulk`; ничего про Content API Wildberries там нет.

**Что решить.** Второй пункт — вопрос к потоку 0: нужен маршрут вида
`/catalog/wb-cards`, либо карточки остаются за отдельным клиентом каталога, и
тогда фраза раздела 6.8 «берёт через `wms`» требует уточнения.

> **ЗАКРЫТО НАПОЛОВИНУ (интеграция) — и вторая половина закрыта быть не может.**
>
> Сторона `wms` сделана целиком: маршрут `POST /catalog/wb-cards` в контракте и
> в обеих реализациях, метод `WbClient.cards` (`POST /content/v2/get/cards/list`
> Content API), Content API в симуляторе стенда. Штрихкод разворачивается из
> `sizes[].skus` — у Wildberries он лежит там, и у одной карточки несколько
> размеров (раздел 3.2). Флаг `mapped` отвечает на единственный вопрос, ради
> которого каталог сюда и ходит.
>
> **Переключение самих `product-catalog` и `wb-returns` остаётся не сделанным**,
> и сделать его отсюда нельзя: их исходников в этом репозитории нет, они лежат
> на `91.108.239.175`, а боевой контур — только чтение до отдельного разрешения
> владельца на каждое изменение (раздел 12). Нужны либо исходники, либо
> разрешение. Это вопрос к владельцу.
>
> Пока не переключено, живо и первое: `catalog-stock-worker` продолжает писать
> остатки в те же кабинеты, что и `wms`. Два сервиса пишут в один кабинет разные
> числа — это надо развести до шага 4 миграции (раздел 11), не позже.

---

# Аудит 11.09.2026

Работа интегратора по промту «исправить все находки аудита mmWMS». Семь
этапов, все находки закрыты кодом и тестом, который до фикса падал.

**Правило, по которому это делалось:** фикс без теста не считается закрытым.
Ни одного `skip` и ни одного `xfail` ради зелёного цвета не добавлено.

## Итог проверок

| Проверка | Результат |
|---|---|
| Ворота стенда (`check-no-live-tokens.sh`) | зелёные, живых токенов нет |
| Юнит-тесты семи сервисов | 475 зелёных, 0 красных, 0 пропущенных |
| `scripts/validate-contracts.sh` | зелёный, без запасного пути |
| `pip-audit` по десяти файлам зависимостей | 0 уязвимостей |
| `ruff` и `mypy` по шести сервисам | чисто |
| Полный прогон раздела 9.6 | **16 из 16** (27 собранных тестов), отчёт в `tests/full-run/reports/` |
| Данные прогона после прогона | 0 объектов в `wms`, 0 в биллинге — проверено счётом до и после |
| Цели Prometheus | 15 из 15 `up` |
| `TasksDiverged`, `WorkerSilent` после прогона | не горят |
| Остановка воркера на 3 минуты | `WorkerDown` сработал, дошёл до приёмника, погас после включения |

Стенд собран из `main`: метка `org.opencontainers.image.revision` каждого из
тринадцати образов совпадает с `git rev-parse HEAD`, и `origin/main` указывает
на тот же коммит.

## Этап 0. Стенд и гигиена данных

| Находка | Что сделано | Коммит | Тест |
|---|---|---|---|
| Три клона репозитория на стенде, контейнеры из разных | Один клон, один compose, стенд собран из него целиком | `340b853` | метка образа = SHA коммита, проверяется в отчёте прогона |
| Юнит-тесты уходили в рабочую базу через фолбэк на `DATABASE_URL` | Фолбэк убран, имя базы обязано оканчиваться на `_test`, иначе `pytest.exit` | `340b853` | `dbfixtures.require_database`, аналогично в conftest пяти сервисов |
| 971 владелец из 997 в рабочей базе — след того же фолбэка | Удалено скриптом, снят `pg_dump` перед уборкой | `340b853` | `docs/schema-decisions.md`, раздел «Тесты не лезут в рабочую базу» |

## Этап 1. Аутентификация

| Находка | Что сделано | Коммит | Тест |
|---|---|---|---|
| 1.1 `identity` не подписывал токены | EdDSA/Ed25519 поверх `cryptography`, JWKS на `/.well-known/jwks.json` | `dc0f8b4` | `identity/tests/test_auth.py` — подделка, `alg: none`, чужой ключ, истёкший |
| 1.2 Ключ подписи жил в памяти процесса | Таблица `identity_signing_key`, миграция 002; в `staging` ключ задаётся явно | `dc0f8b4` | `test_auth.py::test_the_key_survives_a_restart` |
| 1.3 38 маршрутов `wms` открыты | Проверка на роутере целиком, `OPEN_PATHS` — только `/health` | `dc0f8b4` | `wms/tests/test_auth_routes.py` |
| 1.4 Четыре маршрута биллинга открыты, включая `/events` | Право записи либо сервисный токен; `/invoices/{id}` — 404 на чужой | `a1171cf` | `billing/tests/test_rights.py` |
| 1.5 Все маршруты админки открыты, включая `/audit` и `/roles` | Проверка на роутере; подпись берётся из токена, а не из тела | `a1171cf` | `internal-admin/tests/test_admin.py` |
| 1.6 В сокет агента печати приходил кто угодно | Общий секрет, вне локальных сред обязателен | `a1171cf` | `workstation/tests/test_agent_auth.py` |
| ЛК клиента не проверял токен на всех маршрутах | Закрыт целиком; чужой кабинет — «нет такого», а не «нет доступа» | `eac2237` | `portal/tests/test_portal.py::test_the_portal_is_closed_without_a_token` |

## Этап 2. Ядро `wms`

| Находка | Что сделано | Коммит | Тест |
|---|---|---|---|
| 2.1 Каждое живое задание уходило в `diverged` через минуту | `agrees_with_wb(..., in_supply=)`: `confirm` законен для состояний подбора при непустом `supply_id` | `c631e0a` | `test_reconcile.py::test_an_order_inside_a_supply_is_not_a_divergence` |
| 2.2 Отмена у WB не обрабатывалась; сверка читала первую страницу истории | Отмена — команда: резерв снимается, причина разбираема. Сверка спрашивает поимённо `POST /api/v3/orders/status` | `c631e0a` | `test_a_cancellation_at_wildberries_cancels_the_task_and_frees_the_stock`, `test_reconciliation_asks_about_our_own_tasks_not_the_first_page` |
| 2.3 `deliver` не писал ни одного движения; `consumed` не использовался | Движение выхода со стороной `to` пустой, резерв в `consumed` | `7f5a8fe` | `test_delivery_writes_the_goods_out_of_the_warehouse` |
| 2.4 Пересчёт ячейки с резервом давал фантомный излишек | Ожидание = `good + reserved`, дельта к `good` | `7f5a8fe` | `test_counting_a_cell_with_a_reservation_does_not_invent_a_surplus` и парный на настоящую недостачу |
| 2.5 Каждая команда решала сама, из какого состояния выполняться | `ALLOWED_FROM` в `domain.py`, проверка в начале каждой команды | `7f5a8fe` | `test_transitions.py` — 20 проверок, по одной на запрещённый переход |
| 2.6 `return_to_shelf` снимал резерв — товар уезжал по двум заказам | Резерв не трогается: вещь на той же полке и нужна тому же заказу | `7f5a8fe` | `test_returning_to_the_shelf_keeps_the_reservation` |
| 2.7 Ключ идемпотентности только проверялся на непустоту | `command_log` (миграция 009), повтор возвращает сохранённый ответ | `668f45b` | `test_idempotency.py` — 5 проверок, включая второй счёт клиенту |
| 2.8 Отмена между запросом стикера и его записью воскрешала задание | Задание перечитывается под блокировкой; отменённые освобождаются из поставки | `6be810d` | `test_a_cancellation_while_the_sticker_is_being_fetched_does_not_revive_the_task` |
| 2.9 Один заказ с битым полем ронял весь такт опроса | `try/except` на заказ, `manual_review` с кодом `UNPROCESSABLE_ORDER`, курсор в `finally` | `6be810d` | `test_one_poisoned_order_does_not_stop_the_whole_cabinet` |
| 2.10 Девять средних находок ядра | `leased_until` из `RETURNING`, пауза кабинета в `wb_account`, склад обязателен для публикации, подтверждения шины, лизинг outbox, откат резерва по текущему остатку, возврат в поставку, ворота записи в WB | `a62eb2a`, `480113f` | `test_guards_and_limits.py`, `test_transitions.py::test_a_claimed_task_comes_back_with_its_lease` |
| 2.11 Восемь низких находок | Досчёт приёмки, коробка принадлежит клиенту, проверка `doc_type`/`state`, `request_id` вместо текста исключения, `actor_id` листа подбора, явный выбор кабинета | `f8f17bd` | `test_low_findings.py` — 6 проверок |

Версия контракта поднята до **1.3.0**, изменения — в
`services/wms/contracts/CHANGELOG.md`.

## Этап 3. Рабочее место

| Находка | Что сделано | Коммит | Тест |
|---|---|---|---|
| 3.1 Задание в руках пропадало с экрана | `claim: false` с `assignee` отдаёт и свои задания; второй опрос по актору; восстановление сессий при старте | `dfcfde3` | `test_the_screen_keeps_showing_what_a_picker_is_holding`, `test_reading_the_queue_shows_what_is_already_in_hands` |
| 3.2 Двойной клик печатал две этикетки | Ключ печати стабилен, кнопка блокируется; молчание агента — `unknown`, не `failed` | `b310bc1` | `test_a_double_click_prints_once`, `test_a_silent_agent_gives_unknown_not_failed`, `test_an_acknowledgement_from_another_station_is_refused` |
| 3.3 Упавшая база рабочего места держала экран в очереди | Предохранитель на 10 с, `connect_timeout=2`, семафор; подбор не занимает задания при недоступной базе | `9799f37` | `test_a_broken_store_does_not_take_tasks_nobody_can_pick`, `test_a_dead_database_is_not_retried_on_every_request` |
| 3.4 Отказ `wms` показывался как сбой сервиса | `-32602` → `WmsRejected` с текстом; «задания нет» убирает его с экрана | `9799f37` | `test_invalid_params_is_a_refusal_not_an_outage` |
| 3.5 Исходящие запросы не проверялись по контракту | Каждый запрос валидируется по `openapi.yaml`; сверка стикера по заданию, владельцу и заказу | `eb27b0d` | `test_outgoing_contract.py` |
| 3.6 Название товара выполнялось как код на экране | Экранирование, строки собираются узлами, обработчики вешаются кодом | `f1bdec5` | `test_a_product_name_cannot_become_code_on_the_screen` |
| 3.6 Молчащий агент оставался в реестре; кадр терялся на таймауте | Фоновая уборка сессий, возобновляемый разбор кадра | `f1bdec5`, `83d52aa` | `test_a_timeout_in_the_middle_of_a_frame_does_not_desynchronise_the_stream` |
| 3.7 `Store` не проверялся на настоящем SQL | Набор `@pytest.mark.stand` против `workstation_test` | `479abf8` | `test_store_sql.py` — 10 проверок; нашёл две ошибки сразу |

## Этап 4. Биллинг, identity, портал, админка

| Находка | Что сделано | Коммит | Тест |
|---|---|---|---|
| 4.1 `wms_owner_id` оставался пустым; упаковка не тарифицировалась | `owner_id` читается с верхнего уровня; источник упаковки переключён одним оператором | `ec4801a` | `test_tariffs.py`, `FakeWms` валидируется по схеме контракта |
| 4.2 Наценка партнёра удваивалась | `min(depth)` по партнёру; триггер требует точного совпадения раскладки | `134c3b3` | `test_a_senior_on_the_cabinet_and_above_the_manager_is_paid_once` |
| 4.3 Неоплаченное не переигрывалось никогда | `unbilled`/`failed` переигрываются, `accrued` — нет; `POST /unbilled/{id}/replay` | `134c3b3` | `test_an_unbilled_event_is_replayed_once_the_tariff_is_approved` |
| 4.4 Отказ базы отправлял событие в мёртвые письма | Возврат в очередь с паузой, пересоздание пула, след отдельной транзакцией, DLQ объявлена | `26771fd` | `test_consumer.py` — 4 проверки на фейковом pika |
| 4.5 Счета и периоды расходились с бумагой | Счёт за закрытый период, перевыставление только из `issued`, даты в зоне склада, `PERIOD_CLOSED` | `7e31fbe` | `test_a_closed_period_does_not_take_new_accruals`, `test_a_night_shift_event_belongs_to_the_moscow_day` |
| 4.6 Портал и админка показывали разные суммы | `GET /accruals/summary` считает база; курсорная пагинация; акт не исполняемый | `a8ac19a` | `test_the_total_of_a_long_period_is_one_number_for_everyone` (1500 начислений) |
| 4.7 Блокирующие обработчики в цикле событий | 38 обработчиков переведены в пул потоков; кэш интроспекции 30 с | `f37dbc7` | `test_a_blocking_handler_is_not_declared_async` — структурная проверка |

## Этап 5. Наблюдаемость, CI, контракты, документация

| Находка | Что сделано | Коммит | Тест |
|---|---|---|---|
| 5.1 Метрики воркеров никто не забирал | `/metrics` у четырёх воркеров, 15 целей Prometheus, alertmanager с приёмником | `b7afcc0` | приёмник алертов симулятора; проверено остановкой воркера |
| 5.2 Мёртвые письма терялись в никуда | Политика `dead-letter-exchange` на `stand.*` — снаружи, а не аргументом очереди | `b7afcc0` | шаг прогона `test_a_rejected_message_lands_in_dead_letters_not_in_nowhere` |
| 5.3 Ни линтера, ни типов | `pyproject.toml`, job'ы `ruff`/`mypy`, `pip-audit` матрицей, тесты симулятора | `5e94573` | нашли три настоящие ошибки (см. ниже) |
| 5.4 Каталог событий расходился с кодом | `wms.stock.released.v1` издаётся; `wb.orders.processed.v1` не объявлен издаваемым | `fd5fb21` | `test_the_catalogue_and_the_emission_list_agree` |
| 5.5 Прогон гоняли руками | Ночной systemd-таймер с отчётом и SHA; шаг 12 по каждому типу; шаг 14 проверяет возврат консьюмера | `2f8a44e` | отчёт в `tests/full-run/reports/` |
| 5.6 Shadow поднимался, но не отвечал на вопрос недели | `WB_RECONCILE_SKIP_STATES`, preflight спрашивает базу и сервис, README описывает импорт каталога | `9d6d59f` | `test_skipped_states_are_not_even_asked_about` |
| 5.7 Документация описывала вчерашний стенд | README, README стенда, хвост compose, заявка 6 потока B | `393be41` | — |
| 5.8 Миграции, роли, образы, staging | `pg_advisory_lock` и sha256 в журнале, роль на сервис, `apt-get upgrade` убран, наложение staging | `833e72c` | job `compose` в CI |

## Что нашли инструменты, добавленные по ходу

Эти находки не были в списке аудита — их нашли проверки, которые он потребовал
завести.

| Что | Чем найдено | Коммит |
|---|---|---|
| Воркер хранения падал `UnboundLocalError` каждый такт: начисление хранения не проходило вовсе | `ruff` (F823) | `5e94573` |
| Обработчик чужого подтверждения печати звал несуществующий `log` — `NameError` ровно там, где надо было сказать о чужом агенте | `ruff` (F821) | `5e94573` |
| Отключённый клиент без кабинета ронял резерв по `NoneType` | `mypy` | `5e94573` |
| `add_lines` считал попытки, а не вставленные строки: «лист обновился» при нуле изменений | тесты `Store` на настоящем SQL | `479abf8` |
| Исход печати `unknown` не проходил `CHECK` схемы — `finish_print` молча не менял строку | тесты `Store` на настоящем SQL | `479abf8` |
| Сброс симулятора возвращал счётчик поставок назад, если случался в ту же секунду | тесты симулятора | `5e94573` |
| Рабочее место не разбирало шину с этапа 3.6: аргументы очереди неизменяемы, канал закрывался 406 | алерт `WorkstationWorkerSilent` | `a060bf5` |
| Счётчик пропавших заданий не возвращался к нулю — алерт горел при пустой базе | наблюдение за стендом после прогона | `277653a` |
| `WorkerSilent` не срабатывал на ОСТАНОВЛЕННЫЙ воркер: ряд исчезал, сравнивать было не с чем | проверка остановкой воркера (пункт 8 этапа 7) | `6e79f01` |
| Подписка прогона на шину начиналась с шага 6 — приёмка не доезжала до разбора начислений | полный прогон | `4f40df8` |
| Прогон оставлял за собой начисления в биллинге: «0 объектов после прогона» держалось только для `wms` | проверка данных после прогона (пункт 7 этапа 7) | `618da60` |
| PYSEC-2026-1845 в `pytest` 8.3.4 | `pip-audit` | `213aa58` |

## Уборка после прогона: семь находок, которых аудит не видел

Отдельным разделом, потому что нашлись они последними и все одним способом:
проверкой того, что стенд после зелёного прогона чист. Ни один шаг раздела
9.6 этого не проверял — прогон смотрит на то, что создал, и не смотрит на то,
что после себя оставил.

Общее у всех семи — **уборка жаловалась в `print`**, а `pytest -q` при зелёном
прогоне вывод не показывает. То есть она молчала.

| Что | Чем найдено | Коммит | Тест |
|---|---|---|---|
| Уборка биллинга падала на удалении комиссий и до удаления начислений не доходила: отложенный триггер 4.2 на коммите видел начисление с наценкой 15.00 и раскладкой 0.00. За сутки накопилось 267 начислений клиента прогона | счёт строк после прогона | `bbbaace` | `test_the_purge_takes_the_money_of_the_run_seller_with_it` |
| Уборка искала деньги по имени клиента, а биллинг заводит кабинет на `owner_id` из события — владелец у каждого запуска свой. Этих начислений уборка не видела вовсе: 875 начислений и 102 кабинета, по кабинету на прогон | счёт строк после прогона | `bbbaace` | `test_the_purge_finds_the_money_by_the_owner_id_not_only_by_the_name` |
| Сессии подбора не убирались: у `pick_session` нет владельца, связь с прогоном живёт только в строках листа, а строки удалялись первыми. 195 сирот в состоянии `picking` | счёт строк после прогона | `bbbaace` | `test_the_purge_takes_the_picking_sessions_it_opened` |
| Сессия, которой не досталось задания, не оставляет следа и в строках: найти её по владельцу нельзя вовсе | тот же счёт | `bbbaace` | `test_the_purge_takes_a_session_that_never_got_a_line` |
| `python_files = test_full_run.py`: новые проверки оснастки молча не собирались ночным прогоном — то есть не охраняли ничего | запуск прогона целиком (17 тестов вместо 27) | `bbbaace` | сам прогон: 27 собранных |
| Порог наблюдателя за транзакциями стоял на 50 мс и ловил не сеть, а планировщик: прогон покраснел на образце 51.1 мс, где транзакцию держал `INSERT INTO outbox` — последний оператор перед коммитом | полный прогон | `bbbaace` | `test_txnwatch.py` — 4 проверки |
| Тест оставлял партнёра без родителя, то есть **корнем** дерева. Биллинг при нескольких корнях отказывается гадать, на кого записать кабинет, — наценка молча становится нулевой, и видно это только на шаге 12 сообщением «упаковка 30/0/30» | полный прогон | `bbbaace` | `test_the_stand_keeps_exactly_one_root_partner` |

Защита биллинга в последнем случае сработала ровно как задумана: `_default_partner`
отказался угадывать и покрасил прогон. Ошибка была в тесте, который мог
незаметно менять форму дерева партнёров на стенде.

Накопленное убрано разово (`pg_dump` снят перед этим): 102 кабинета прогона,
876 начислений, 194 осиротевшие сессии. После следующего прогона счёт строк в
обеих базах не изменился ни на единицу — уборка делает то, что о ней написано.

Чего эта проверка по-прежнему не видит: вызов в **локальный** симулятор
занимает около 10 мс и не перешагнёт ни старый порог, ни новый. Наблюдение
калибровано на настоящий шлюз (≈500 мс); строгая проверка «в спане транзакции
нет дочерних HTTP-спанов» ждёт трассировки от потока A. Это ограничение было
и до калибровки и названо в описании класса.

## Хранение не начислялось никому с этапа 1

Нашлось последним и по одному горящему алерту: `UnbilledGrowing` не гас после
прогона. Причина оказалась не в прогоне.

Этап 1 закрыл все 38 маршрутов склада (находка 1.3). Воркер хранения биллинга
ходит в `wms` за коробко-местами — и токена не посылал. С того дня он получал
**401 на каждый кабинет и на каждый досчитываемый день**: 162 строки «не
дошло до счёта» за один такт, каждые пятнадцать минут. Хранение не
начислялось ни одному клиенту вообще.

Не заметили этого потому, что отказ склада считался **отсутствием
количества**. Причина `NO_QUANTITY` читается как «у клиента не стояло
коробок» — нормальное состояние пустого стенда. Дежурный по ней идёт смотреть
склады, а смотреть надо было токен.

| Что | Коммит | Тест |
|---|---|---|
| Воркер хранения представляется складу сервисным токеном | `2e0d823` | `test_the_storage_worker_introduces_itself_to_the_warehouse` |
| `billing-worker` получает `SERVICE_TOKEN` в compose — без него код бессилен | `2e0d823` | тот же тест на стенде: запрос отвечает 200, а не 401 |
| Отказ склада получил свою причину `WAREHOUSE_UNAVAILABLE` и больше не маскируется под «нет количества» | `2e0d823` | `test_a_warehouse_that_refuses_is_not_reported_as_missing_quantity` |
| Воркер логировал только начисленное: пока хранение не начислялось никому, в логе было пусто | `TBD` | `test_the_storage_worker_says_out_loud_when_it_did_not_accrue` |
| Воркер тикал, не дождавшись склада: на каждом подъёме стенда — сутки хранения «недоступен» по всем кабинетам сразу | `TBD` | `depends_on: wms: service_healthy`; прирост счётчика после подъёма — ноль |

После починки `/storage/lookup` отвечает 200 и на неизвестного склада
клиента — пустым списком мест, а не отказом. Кабинет без коробок за хранение
не платит и в «не дошло до счёта» не идёт, как и задумано.

**Чего это не закрывает.** Полный прогон хранение не проверяет вовсе — в
шестнадцати шагах раздела 9.6 такого шага нет. Поэтому дефект и прожил сутки
при зелёном прогоне. Шаг «клиент, у которого коробки простояли сутки, получил
начисление за хранение» стоит завести, но это расширение раздела 9.6, а не
закрытие находки аудита: решение о составе прогона — владельца.

## Чего НЕ сделано

**1. Роли Postgres не включены на стенде.**
`infrastructure/stand/scripts/create-roles.sh` написан и идемпотентен, но
сервисы продолжают ходить под владельцем баз: переключение требует девяти
паролей снаружи Git и одновременной правки `compose.yaml`. Это изменение
конфигурации стенда, а не кода, и делать его в одном коммите с аудитом
значило бы уронить стенд посреди проверки. Скрипт готов; включение — отдельный
шаг с владельцем.

**2. Вопросы раздела 13 остаются открытыми — их закрывает владелец.**
Ни один не закрыт интегратором, и это правильно. Состояние — таблица раздела
13 мастера. Блокирующий прямо сейчас один: **вопрос 11** (частота опроса
боевого шлюза) — без него не поделить лимит 300 запросов в минуту, и шаг 2
миграции не начать.

**3. Допущение интегратора, записанное в раздел 13 (пункт 13).**
Сборщик у рабочего места набирает себя руками: входа и пользователей у него
нет. Имя разворачивается в постоянный `uuid5` — одинаково в `wms` и в рабочем
месте. Контракт соблюдён, «за кем задание» воспроизводимо, но настоящего
ответа «кто это сделал» это не даёт: выведенный идентификатор ни с кем в
`identity` не связан. Закрывается входом сборщика по токену.

**4. Формат этикетки не подтверждён живым принтером** (вопрос 2 раздела 13).
На шаге 2 миграции не мешает — стикеры там не запрашиваются. К шагу 5 обязан
быть закрыт. Прогон больше не вписывает `zplv` жёстко: формат берётся из
`WB_STICKER_FORMAT`, и переключение на `png` не покрасит прогон.

**5. `product-catalog` и `catalog-stock-worker` живут на боевом сервере.**
Раздел 6.8 отдал чтение Content API складу, но `catalog-stock-worker`
продолжает писать остатки в те же кабинеты. На шаге 2 безвредно — в shadow мы
не публикуем ничего. **До шага 4 развести обязательно**, иначе два сервиса
будут писать в один кабинет разные числа. Нужны исходники платформы либо
разрешение менять бой.

**6. Исторические строки «не дошло до счёта» в биллинге стенда.**
27 строк от 10.09.2026 — события стикеровки, выпущенные до того, как `wms`
начал класть владельца в payload. Помечены разобранными с объяснением
(`pg_dump` снят перед правкой). Задания, которые они описывают, удалены;
начислить по ним нечего.

## Стоп-линия

Дальше пункта «поднять shadow-опрос, ничего не публикуя» не пройдено ни на
шаг. Боевой сервер `91.108.239.175` не открывался ни разу: ни чтения, ни
записи. Сервер `185.73.126.232` не трогали.
