"""Детерминированное окружение для тестов mock-сервиса wms."""
import os

os.environ["APP_ENV"] = "test"
os.environ["WMS_MOCK"] = "true"
# Брокера в юнит-тестах нет намеренно: склад не должен зависеть от шины
# (раздел 6.1), и тесты обязаны это подтверждать, а не обходить.
os.environ.pop("RABBITMQ_URL", None)
