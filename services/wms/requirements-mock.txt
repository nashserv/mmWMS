# Зависимости ТОЛЬКО для mock потока 0. Снимаются вместе с mock, когда поток A
# сдаёт настоящий сервис. Основной requirements.txt при этом не трогается —
# он повторяет замороженный набор платформы (раздел 2.2 мастера).
-r requirements.txt
# Нужен тесту соответствия mock контракту: события проверяются схемой
# из самого asyncapi.yaml, а не её копией в тесте.
jsonschema==4.23.0
PyYAML==6.0.2
