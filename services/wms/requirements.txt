fastapi==0.141.1
starlette==1.3.1
uvicorn[standard]==0.34.2
cryptography==50.0.0
httpx==0.28.1
psycopg[binary]==3.2.9
prometheus-client==0.21.1
# Публикация событий напрямую в RabbitMQ. Раздел 6.7: собственный outbox
# сервиса несёт события в шину сам, без общего публикатора платформы, —
# значит, клиент AMQP нужен самому сервису, а не только его заглушке.
pika==1.3.2
