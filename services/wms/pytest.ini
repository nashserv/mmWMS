[pytest]
pythonpath = . tests
testpaths = tests
# -ra печатает причины пропусков в итоге. Тесты ядра идут против настоящего
# Postgres, и без него они пропускаются: пропуск читается как успех, поэтому
# он обязан быть виден в выводе CI, а не теряться в точках.
addopts = -ra
