# Шина — ускоритель UI, а не источник истины (раздел 6.1 мастера). Поэтому
# зависимость брокера вынесена отдельным файлом: рабочее место обязано
# собираться и работать без неё, и сборка без pika — это проверка, а не
# экономия. Основной requirements.txt при этом не трогается.
-r requirements.txt
pika==1.3.2
