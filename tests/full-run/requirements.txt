# Зависимости полного прогона. Версии те же, что у сервисов платформы
# (раздел 2.2 мастера) — новых библиотек прогон не вводит.
pytest==8.3.4
httpx==0.28.1
psycopg[binary]==3.2.9
# Чтение mmx.events: события проверяются на шине, а не в служебном окне mock —
# потоки B и C подписаны именно на обменник.
pika==1.3.2
