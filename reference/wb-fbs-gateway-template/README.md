# Шаблон сервиса — снято с `wb-fbs-gateway`

**Только чтение.** Не собирать, не запускать, не деплоить. Это справочный материал для потока 0 (`docs/01-stream-0-contracts-and-stand.md`, пункт 8 — «Шаблон сервиса и линтер»), чтобы `services/wms/` был написан в том же стиле, что и остальные девять сервисов платформы.

Снято `10.09.2026` с `91.108.239.175`, `/root/MM-express-final-platform-integration/services/wb-fbs-gateway/`, read-only SSH. Секретов в файлах нет — проверено grep'ом перед коммитом; значения вида `development-only-*` и `change-me` — намеренные dev-заглушки из самого исходника, не настоящие ключи.

## Что здесь и зачем

| Файл | Зачем |
|---|---|
| `Dockerfile` | Точный рецепт образа: `python:3.13-slim` с digest-пиннингом, `USER 65532`, порядок `COPY`. Копировать как есть, менять только `CMD`/имя пакета |
| `requirements.txt` | Точные версии зависимостей — не переизобретать, взять эти |
| `pytest.ini` | Конфиг тестов |
| `.gitignore` | Что не коммитить |
| `compose.yaml` | Как сервис описывает себя для локального/дев-окружения — секреты через `secrets:`/файлы, не переменные с реальным значением |
| `app/__init__.py`, `app/config.py` | Как читаются секреты (`read_secret`) и настройки — с диска/файла, не из голого env, где это могло бы утечь в `docker inspect` |
| `app/domain.py` | Форма доменных моделей (dataclass-стиль, что как называется) |
| `app/metrics.py` | Как заведены метрики Prometheus и коды ошибок как именованные константы, не голые строки |
| `app/provider_fence.py` | Паттерн изоляции внешнего вызова — пригодится потоку A для границы вызовов Wildberries |
| `tests/conftest.py` | Минимальный сетап тестов |
| `tests/test_config.py`, `tests/test_metrics.py` | Стиль тестов: `unittest`/`pytest`, синтетические фикстуры (`synthetic_token()`), явная проверка, что секрет не утёк в ответ (`assertNotIn(self.token, ...)`) |
| `migrations/001_initial.sql` | Один пример миграции — как называть файлы, как оформлять |

## Осознанно не взято

- `.venv/` — в исходнике почему-то закоммичен виртуальный окружение; не тащить эту практику дальше
- Бизнес-логика WB (`service.py` 191 КБ, `wb.py` 83 КБ, `workflow.py`, `lifecycle.py`, `accounts.py`) — это домен, который поток A пишет заново под свою транзакционную модель (раздел 6.2 мастер-контекста), не копирует
- Отдельного файла линтера/форматтера в исходнике нет — по `.github/workflows/ci.yml` видно, что CI просто ставит `requirements.txt + pytest + pip-audit` и гоняет тесты против настоящего Postgres с применёнными миграциями по цепочке, плюс валидирует контракты (`openapi.yaml`/`asyncapi.yaml`) отдельным Node-инструментом (`tools/contract-validation`) и `scripts/validate_event_contracts.py`. Поток 0 может взять этот же подход для `services/wms/`, а не выдумывать новый.
